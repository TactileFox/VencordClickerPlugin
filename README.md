# VencordClickerPlugin
A Discord user plugin for Vencord.dev. It plays a user-given sound client-side when a message matches user-given regex.
Users can add custom regex and audio files with whitelist/blacklist functionalities.  The audio plays when a message from a whitelisted source matches the user-given regex pattern.
